# appimage-builder recipe see https://appimage-builder.readthedocs.io for details
# appimage-builder --recipe AppImageBuilder.yml
# wget -O appimage-builder-x86_64.AppImage https://github.com/AppImageCrafters/appimage-builder/releases/download/v1.1.0/appimage-builder-1.1.0-x86_64.AppImage
# chmod +x appimage-builder-x86_64.AppImage

# # install (optional)
# sudo mv appimage-builder-x86_64.AppImage /usr/local/bin/appimage-builder
version: 1
script:
  - rm -rf AppDir || true
  - cp -r build/linux/x64/release/bundle AppDir
  - mkdir -p AppDir/usr/share/icons/
  - cp icons/production/driftfin_icon_desktop.png AppDir/usr/share/icons/
AppDir:
  path: ./AppDir
  app_info:
    id: io.github.hamadtheironside.driftfin
    name: Driftfin
    icon: driftfin_icon_desktop
    version: latest
    exec: driftfin
    exec_args: $@
  files:
    include:
      - assets/fonts/rubik/Rubik-Italic-VariableFont_wght.ttf
      - assets/fonts/rubik/Rubik-VariableFont_wght.ttf
      - icons/production/driftfin_icon_desktop.png
AppImage:
  arch: x86_64
  update-information: gh-releases-zsync|HamadTheIronside|Driftfin|latest|Driftfin-Linux-*.AppImage.zsync
  file_name: Driftfin_x86_64.AppImage
